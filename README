# Dependency Tree Script

The script "test_script.py" reads sequence, dependencies and sequence names from CSV files, builds a dependency tree using networkx, and prints the order of execution.

## Requirements
- Python
- pandas
- networkx
- openpyxl
- xlsxwriter

## Installation
pip install -r requirements.txt

## Perform unittest
To run the unit tests, use the following command:
```sh
python -m unittest test_script.py
```